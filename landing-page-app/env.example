# 🚀 TransQA Landing Page Environment Configuration
# Copy this file to .env and customize your settings

# === BASIC SETTINGS ===
# Your domain name (required for production)
DOMAIN=localhost

# HTTP/HTTPS ports
HTTP_PORT=80
HTTPS_PORT=443

# === APPLICATION SETTINGS ===
# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Number of worker processes
WORKERS=4

# === LANGUAGETOOL SETTINGS ===
# Memory allocation for LanguageTool
LANGUAGETOOL_MEMORY=2g

# === SSL SETTINGS (Optional) ===
# Email for SSL certificate registration
# SSL_EMAIL=admin@yourdomain.com

# === ADVANCED SETTINGS ===
# Database connection (if using external DB)
# DATABASE_URL=postgresql://user:pass@host:port/db

# External LanguageTool URL (if using external service)
# LANGUAGETOOL_URL=http://your-languagetool-server:8010

# === MONITORING & METRICS ===
# Enable metrics collection
# ENABLE_METRICS=true

# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn

# === DEVELOPMENT SETTINGS ===
# Enable debug mode (only for development)
# DEBUG=false

# Enable hot reload (only for development)  
# HOT_RELOAD=false
