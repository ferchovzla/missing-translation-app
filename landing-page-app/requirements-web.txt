# FastAPI and web dependencies for TransQA Landing Page
fastapi==0.104.1
uvicorn[standard]==0.24.0
jinja2==3.1.2
python-multipart==0.0.6
aiofiles==23.2.1

# TransQA base dependencies (duplicated to avoid path issues)
# Core dependencies
pydantic>=2.0,<3.0
typer[all]>=0.9.0,<1.0
rich>=13.0,<14.0
toml>=0.10.2 ; python_version < "3.11"
tomli>=2.0.1 ; python_version < "3.11"

# Web scraping and text extraction  
requests>=2.31.0,<3.0
trafilatura>=1.6.0,<2.0
beautifulsoup4>=4.12.0,<5.0

# Language detection and NLP
langid>=1.1.6,<2.0

# Optional dependencies for verification (minimal set)
# Note: LanguageTool and other heavy dependencies will be handled by services
